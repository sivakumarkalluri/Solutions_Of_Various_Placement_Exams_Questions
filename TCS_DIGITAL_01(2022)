
N=int(input())
L=int(input())
array=[]
for i in range(N):
    array.append(int(input()))
result=[]
array=sorted(array)
for i in range(N):
    if array[i]==L:
        result.append(i)
if len(result)!=0:
    print(result[0],result[-1])
else:
    print("Not found")
        
    
